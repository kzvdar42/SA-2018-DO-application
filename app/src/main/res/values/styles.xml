<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.Toolbar">
        <item name="android:windowBackground">@color/colorPrimary</item>
    </style>

    <!-- Cards theme. -->
    <style name="AppTheme.CardViewStyle">
        <item name="contentPaddingBottom">24dp</item>
        <item name="contentPaddingTop">24dp</item>
        <item name="contentPaddingLeft">16dp</item>
        <item name="contentPaddingRight">16dp</item>
    </style>

    <!-- Recycle view theme. -->
    <style name="AppTheme.recycleViewItem">
        <item name="android:clickable">true</item>
        <item name="android:focusable">true</item>
        <item name="android:foreground">?android:attr/selectableItemBackground</item>
    </style>

    <!-- Button theme. -->
    <style name="AppTheme.Button">
        <item name="android:padding">12dp</item>
        <item name="cornerRadius">12dp</item>
        <item name="android:textSize">15sp</item>
        <item name="android:textAllCaps">false</item>
    </style>

    <!-- Navigation. -->
    <style name="NavigationMapRoute">
        <!-- Colors -->
        <item name="routeColor">@color/mapboxBlue</item>
        <item name="routeModerateCongestionColor">@color/mapboxYellow</item>
        <item name="routeSevereCongestionColor">@color/mapboxPink</item>
        <item name="routeShieldColor">@color/mapboxWhite</item>
        <!-- Scales -->
        <item name="routeScale">1.0</item>
    </style>

    <style name="NavigationView" parent="NavigationViewLight">
        <item name="navigationViewPrimary">@color/mapboxWhite</item>
        <item name="navigationViewSecondary">@color/colorAccent</item>
        <item name="navigationViewAccent">@color/colorAccent</item>
        
        <item name="navigationViewBannerBackground">@color/colorPrimary</item>
        <item name="navigationViewBannerSecondaryText">@color/color_black</item>
        <item name="navigationViewBannerPrimaryText">@color/color_black</item>
        <item name="navigationViewBannerManeuverPrimary">@color/colorAccent</item>
        <item name="navigationViewBannerManeuverSecondary">@color/colorPrimaryDark</item>
        <item name="navigationViewDivider">@color/colorPrimaryDark</item>

        <item name="navigationViewListBackground">@color/colorPrimarySoftDark</item>
    </style>

</resources>
